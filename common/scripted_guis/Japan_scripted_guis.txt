scripted_gui = {
	JAP_superevents_view = {
		context_type = player_context
		window_name = "JAP_superevents_window"
		ai_enabled = { always = no }
		visible = {
			has_variable = superevents_numbers
			is_ai = no
		}
		triggers = { superevents_pictures_visible = { has_variable = superevents_numbers } }
		properties = { superevents_pictures = { image = "[GetSupereventsPictures]" } }
		effects = { superevents_remove_button_click = { clear_variable = superevents_numbers } }
	}
}
