	on_war = { #copied over the template for now just change it apply to cosmetic tags instead also remember don't need to repat the PHI and PHW that already exists in the kx files in global on action. Just change what PHI and PHW has as flags
		effect = {
			if = {#could be fucky cause we have the kaiserredux base 
				limit = {
					tag = PHI
					NOT = {
						has_country_flag = PHI_at_war
						has_global_flag = PHI_SOCIALIST
					}
				}
				rd_phil_change_flag_peace_war = yes
				set_country_flag = PHI_at_war
			}
		}
	}
	on_peace = {
		effect = {
			if = {
				limit = {
					tag = PHI
				}
				if = {
				limit = {
					has_country_flag = PHI_at_war
					NOT = { has_global_flag = PHI_SOCIALIST }
				}
				rd_phil_change_flag_war_peace = yes
				clr_country_flag = PHI_at_war
				}
				if = {
				limit = {
				has_country_flag = RD_PHI_JAP_INVADE_ME 
				NOT = { has_country_flag = RD_PHI_JAP_DONE_WAR }
				}
				set_country_flag = RD_PHI_JAP_DONE_WAR 
				country_event = { id = rd_phil.6 days = 10 }
				}


			}
		}
	}


on_war_relation_added = {
    effect = {
	if = {
	limit = { 
	tag = JAP
	FROM = { 
	tag = PHI 
	NOT = { has_country_flag = RD_PHI_JAP_INVADE_ME }
	}
	}
	FROM = {
	set_country_flag = RD_PHI_JAP_INVADE_ME 
	country_event = rd_phil.5
	}



	}
	}
}

