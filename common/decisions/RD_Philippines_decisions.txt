political_actions = { 
	PHI_soc_csa_puppet_des = { # america
		icon = GFX_decision_generic_nationalism
		fire_only_once = yes
		allowed = { tag = PHI }
		available = {
			CSA = {
			is_subject_of = PHI
			}
		}
		visible = { 
			CSA = {
			is_subject_of = PHI
			}
		}
		fire_only_once = yes
		cost = 50
		ai_will_do = {
			factor = 10
			}
		complete_effect = {
			CSA = {
			if = {
			limit = {
			owns_state = 361
			}
			set_capital = { state = 361 }
			}
			set_cosmetic_tag = PHI_csa_puppet
		}
	}
	}

PHI_form_malaysia = { #just testing for now, no loc no behavior, no conception should later be a separate decision category
		icon = ger_military_buildup
		allowed = {tag = PHI }
		available = {
		MAL = { exists = no }
		INS = { exists = no }	
		}
		
		cost = 100
		fire_only_once = yes
		days_remove = 120
		ai_will_do = {
			factor = 10
			}

		complete_effect = {
			
		}
		remove_effect = {
		every_state = {
		limit = {
		OR = { is_indonesia = yes state = 336 state = 846}
		}
		add_core_of = PHI
		}
		

		}
	}
	
}


PHI_emilio_politics_decisions = {
	
	PHI_emilio_leftism_stuff = {
		icon = GFX_decision_generic_independence

		available = {
			NOT = {
				has_country_flag = NFL_doing_decision
			}
		}

		visible = {
			event_target:NFL_benefactor = {
				exists = yes
			}
		}
		
		ai_will_do = {
			factor = 0
		}

		cost = 50
		days_re_enable = 20
		fire_only_once = no
		days_remove = 35

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CAN_showcase_british_culture"
			set_country_flag = NFL_doing_decision
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove CAN_showcase_british_culture"
			clr_country_flag = NFL_doing_decision
			NFL_improve_newfie_influence_small_bop = yes
			add_political_power = -60
			add_stability = 0.05
		}

		ai_will_do = {
			factor = 50
		}
	}
	PHI_emilio_rightist_stuff = {
		icon = GFX_decision_generic_military

		available = {
			NOT = {
				has_country_flag = NFL_doing_decision
			}
		}

		visible = {
			event_target:NFL_benefactor = {
				exists = yes
			}
		}
		
		ai_will_do = {
			factor = 0
		}

		cost = 50
		days_re_enable = 30
		fire_only_once = no
		days_remove = 40

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CAN_showcase_british_culture"
			set_country_flag = NFL_doing_decision
		}
		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove CAN_showcase_british_culture"
			clr_country_flag = NFL_doing_decision
			NFL_improve_canadian_influence_small_bop = yes
			add_manpower = 1200
		}

		ai_will_do = {
			factor = 50
		}
	}

}