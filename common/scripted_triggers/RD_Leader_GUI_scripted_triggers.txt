uses_leader_gui = {
	OR = {
		AND = {
			tag = CAN
			NOT = {
				has_country_flag = CAN_is_republic
			}
		}
		
		
		
		
		has_idea_with_trait = vice_president
		has_idea_with_trait = taoiseach
		
		JAP_has_leader_GUI = yes
		
		has_country_flag = LEADER_GUI_VISIBLE
	}
}


JAP_has_leader_GUI = {
	OR = {
		has_country_flag = JAP_EMPEROR_HIRO
	}
}
